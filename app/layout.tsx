import React from "react"
import type { Metadata, Viewport } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'
import './mobile-device-frame.css'

const _geist = Geist({ subsets: ["latin"] });
const _geistMono = Geist_Mono({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: 'FlowGuard - Flood Early Warning System',
  description: 'Mobile-first flood early-warning application for Ho Chi Minh City. Predicts flooding 5-10 minutes before occurrence through IoT sensor data fusion, weather information, and citizen feedback.',
  keywords: ['flood warning', 'Ho Chi Minh City', 'Vietnam', 'disaster prevention', 'IoT sensors', 'early warning system'],
  authors: [{ name: 'FlowGuard Team' }],
  icons: {
    icon: [
      {
        url: '/icon-light-32x32.png',
        media: '(prefers-color-scheme: light)',
      },
      {
        url: '/icon-dark-32x32.png',
        media: '(prefers-color-scheme: dark)',
      },
      {
        url: '/icon.svg',
        type: 'image/svg+xml',
      },
    ],
    apple: '/apple-icon.png',
  },
}

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
  themeColor: '#289359',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
